export interface RoutesType {
  LOGIN: string;
  REGISTER: string;
  DASHBOARD: string;
  TRANSFER: string;
  DEPOSIT: string;
  EXCHANGE: string;
  HISTORY: string;
  SAVINGS: string;
  PROFILE: string;
}

export const ROUTES: RoutesType = {
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
  TRANSFER: '/transfer',
  DEPOSIT: '/deposit',
  EXCHANGE: '/exchange',
  HISTORY: '/history',
  SAVINGS: '/savings',
  PROFILE: '/profile',
}
